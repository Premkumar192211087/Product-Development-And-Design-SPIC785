package com.example.stockpilot;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class AddEditVendorActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_add_edit_vendor);

        // Initialize UI components and handle logic for adding/editing vendors
    }
}
